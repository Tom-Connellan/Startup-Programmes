#Task 4

#Input the integer
integer = int(input("Input the number to be divided: "))

#Input the divisor
divisor = int(input("Input the divisor: "))

#calculate the number and remainder
print(str(integer//divisor) + " remainder " + str(integer%divisor))